# Handoff: crystal-portal session 2 (continuing from part-payments work)

## Repo context
`C:\Users\Crystal Emb Tower\OneDrive\Desktop\MY APPS\crystal-portal` — Cloudflare Pages app. `admin.html` is the whole frontend (vanilla JS + Tailwind, no build). `functions/api/*.js` are the backend (Pages Functions, D1 + R2). No migration tool — schema evolves via `CREATE TABLE IF NOT EXISTS` + guarded `ALTER TABLE ADD COLUMN` on every request. Deployed by pushing to `main` (GitHub-connected Cloudflare Pages, repo `farja-king/crystal-portal`). Live domain: `portal.embroidery.click`. A separate Worker `crystal-inbox-worker` (source `email-worker/worker.js`) runs a ~15-min cron sweep calling back into the Pages API via `X-API-Key`/`PORTAL_API_KEY`.

I (Claude) have **live Cloudflare API access** via connected MCP tools (`mcp__a672f508...`) — can query D1 directly (`d1_database_query`), list/create R2 buckets, etc. Account has one D1 db (`crystal-portal-db`, uuid `8af93eb8-353f-4c9a-be62-d60ee95cb85c`) and two R2 buckets: `crystal-portal-customer-designs` (bound as `DESIGN_FILES`) and the new `crystal-portal-backups` (bound as `BACKUPS`, both Production and Preview — Martin confirmed both bindings saved, Production redeployed successfully).

## Everything shipped this session, roughly in order

**Part-payments project (4 phases, all shipped):**
1. `payments` ledger table, Record/Edit Payment modal, payment history in order view, `orders.amount_paid`/`paid_status` (`'unpaid'|'partial'|'paid'`).
2. Payment receipts (`functions/api/receipt.js`) + per-invoice statement (`functions/api/statement.js`) + customer-wide statement across all invoices (`functions/api/customer-statement.js`) — all three now have a **preview-before-send** step (`preview:true` body flag, shown in a modal iframe, only sent on explicit confirm).
3. Deposits on invoices (`deposit_pct`/`deposit_amount` on orders) — shown on PDF/email as "Deposit due" on an invoice's **first send only** (tracked via `email_sent_count`), then "Paid to date/Balance due" on every send after. Quotes always show "Deposit due on acceptance" (no first-send restriction). `convert_to_invoice` resets `email_sent_count` so a converted invoice gets its own genuine first-send deposit ask.
4. Dashboard revenue reworked to bucket by each **payment's own `received_at`** (new `functions/api/payments.js?all=1`), not `orders.paid_at` — revenue tile, chart, and Top Customers (capped at top 8, now shows a "+N more, totalling £X" line since that cap was confusing Martin).

**Other fixes/features this session:**
- Fixed a real qty/total pricing bug: customer's-own-saved-catalog-item lines wrote qty to `breakdown[0].qty` but server pricing only read `item.qty` for non-garment lines — fixed in `priceItems()` (orders.js).
- Backfill migration: invoices marked Paid via the old pre-ledger one-click toggle had `paid_status='paid'` but zero `payments` rows — one-time backfill in orders.js inserts a synthetic payment for those.
- Customer Directory Activity column / edit form / Customer View panel all now show **combined** Square-baseline + portal-invoice activity (`combinedCustomerActivity()` in admin.html, `portal_*` fields added by `functions/api/customers.js` GET). Removed the old manually-editable Lifetime spend/Transaction count/Last visit fields entirely (business is moving off Square) — now a read-only computed line.
- Pause/Resume Reminders per invoice (`reminder_paused` column, row-action button, `🔕 Paused` badge) — full opt-out from the automatic daily chase, distinct from `reminder_interval_days` (which only changes cadence).
- Renumber Invoices / Renumber Quotes buttons (Quotes & Invoices toolbar) — corrects the `counters` table after test data, via `set_invoice_counter`/`set_quote_counter` PUT actions. Never touches existing records.
- Email button now says "Email Quote"/"Email Invoice" instead of generic "Email".
- Collapsible Custom Price List in Customer View (collapsed by default, uniform for every customer) — was pushing everything else far down the page for customers with long lists (e.g. Karl Sports).
- **Full system backup & restore** (`functions/api/backup.js`, new "Backups" tab in admin.html): backs up every D1 table except `auth_config` (credentials excluded) plus every R2 file referenced by a D1 row, into the `BACKUPS` bucket under a timestamped prefix. Self-throttled to ~once/24h off the existing 15-min cron (added `sweep("/api/backup")` to `email-worker/worker.js`). Restore always takes a fresh safety backup first and requires typing an exact confirmation phrase (server-validated). **This is now live and working** — Martin did the Pages binding setup, tested "Backup Now" successfully.
- Found and fixed a real gap: deleting a quote/invoice (orders.js DELETE handler) cleaned up design proofs/production photos correctly but **never cleaned up `payments` or `email_log` rows** — fixed going forward (`deleteOrderPaymentsAndLogs`). Extended the existing "Clean Up Old Files" button to also purge the *existing* backlog of orphaned payments/email_log rows (new `?orphaned_payments=1`/`?orphaned_email_log=1` GETs + `purge_orphaned_payments`/`purge_orphaned_email_log` DELETE actions in orders.js). Added a genuine (hard, not soft) "Clear inbox history for this customer" button in Customer View (`purge_customer_history` DELETE action in `functions/api/inbox.js`), scoped by `customer_id`, customer record itself untouched.

## Important context for the next session
- **TEST CUSTOMER** = id `81f31f4c-2a47-4566-8dfe-30f3dccaf540`, email `martinlyon70@gmail.com` — this is Martin's own test account, used heavily throughout this whole session. Its quotes/invoices have already been deleted by Martin directly in the app. **Do NOT delete the customer record itself** — only asked to clear history/leftovers.
- There's also a second, separate customer literally named **"test"** (blank email, id `7c82614e-d230-4966-a1ea-f956e529792a`) and **Kyle Saunders** (id `86a2e539-cc25-441a-af96-05b9f06fb2ed`, email `kyle.saunders3@icloud.com`) — Martin explicitly said **leave both of these alone**, only TEST CUSTOMER was in scope for cleanup.
- Last known state of the cleanup: Martin clicked "Clean Up Old Files" — unconfirmed whether the orphaned payments/email_log backlog was fully purged (worth checking `SELECT COUNT(*) FROM payments WHERE order_id NOT IN (SELECT id FROM orders)` and same for `email_log` if picking this up). He also hasn't yet used the new "Clear inbox history for this customer" button on TEST CUSTOMER's 2 remaining inbox_emails rows.
- Old backup snapshots (taken before the cleanup) will still show the old messy test data inside them — **this is correct, expected behavior**, not a bug (a backup is a frozen point-in-time snapshot; already explained to Martin, he understood).
- Rule I'm following throughout: I never execute permanent/destructive deletes myself (even though I have live DB write access via MCP tools) — I build the feature/button and Martin clicks it himself. Keep doing this for anything genuinely destructive.
- Martin operates mostly via voice-to-text dictation — expect occasional garbled phrasing/typos in his messages (e.g. "government record" for "garment record", "Carl Sports" for "Karl Sports") and use context to interpret intent rather than taking every word literally.

## Suggested next steps
- Confirm with Martin whether the orphaned payments/email_log cleanup fully worked, and whether he's used the inbox-history-clear button on TEST CUSTOMER yet.
- Otherwise, no other open threads — everything requested this session has shipped. Ask Martin what's next.
